{
  "name": "w4trc-forms",
  "private": true,
  "dependencies": {
    "@sentry/browser": "^8",
    "@sentry/cloudflare": "^8"
  },
  "scripts": {
    "build": "esbuild sentry-browser-entry.js --bundle --minify --format=iife --global-name=Sentry --outfile=public/sentry.min.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
